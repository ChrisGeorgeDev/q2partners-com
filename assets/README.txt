Q2 Capital Partners — Asset Notes

/backgrounds/  — Original high-resolution source photos for each Communities card and
                 Companies column. Optimize from these (WebP/JPEG, responsive sizes).
                 File names match the section they appear in.

/logos/        — The FINAL processed logo art exactly as used on the site (cream/white
                 knockouts, transparent backgrounds, cropped to emblems where applicable).
                 Drop-in ready — no re-editing needed. Kennedy Circle is split into two
                 files (emblem + wordmark). q2-capital-partners-brand-logo.png is the
                 header/footer mark.

index.html currently has all of the above embedded inline as base64 (works as-is). To
externalize, replace the data: URIs with paths into /assets/. See HANDOFF.md.
